Met nombreMetodo () {
      numero = int () ;
      numeroflotante = float ();
      caracter = String ();
      numero1 = int (10);
      numero2 = float (0.3);
      caracter1 = String ("hola");
      for(i to 5){
        write: "El numero si es mayor ",numero1;
      }
}
